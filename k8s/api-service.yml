apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: hybrid-rag
  labels:
    app.kubernetes.io/name: api
    app.kubernetes.io/part-of: hybrid-rag
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: api
  ports:
    - port: 8080
      targetPort: 8080
      name: http
